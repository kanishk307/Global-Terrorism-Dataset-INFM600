cor.test(data$nkill, data$propextent)
plot(data$nkill, data$propextent)
abline(lm(data$nkill~data$propextent) )
data1 <- read.csv(file.choose())
data2 <- subset(data1, data1$nwound > 10)
View(data2)
data2 <- subset(data1, data1$nwound > 50)
data3 <-subset(data2, data2$nkill == 0)
grp <- subset(data, gname == 'Taliban')
grp1 <- subset(grp, grp$country_txt == 'Afghanistan')
grp1 <- subset(grp, grp$country_txt != 'Afghanistan')
iraq <- subset(data, country_txt == 'Iraq')
count(iraq, 'gname')
